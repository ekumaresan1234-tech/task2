# Task Manager Django App - Implementation TODO

## Approved Plan Steps (progress tracked):

✅ **1. Setup virtual environment and install Django** (commands provided below)

## Remaining Steps:
✅ **2. Create Django project `taskmanager` and app `tasks`**
✅ **3. Define `Task` model in `tasks/models.py`**
✅ **4. Create views (CBV + AJAX) in `tasks/views.py`**
✅ **5. Setup URLs: `tasks/urls.py` & `taskmanager/urls.py`**
✅ **6. Create templates: `base.html`, `task_list.html`, `task_form.html`**
✅ **7. Configure `admin.py`, `settings.py`**
- [ ] 8. Run migrations, create superuser
- [ ] 9. Test & complete (runserver)

**Next**: Execute setup commands below after confirming venv activation.

**Run these in new terminal:**
```
cd "c:/Users/AKASH/Desktop/TASK MANAGER"
python -m venv venv
venv\Scripts\activate
pip install django
```
Copy-paste the output/confirmation.

